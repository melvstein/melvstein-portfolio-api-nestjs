APP_NAME=melvstein-portfolio-api-nestjs

# NODE_ENV = development, staging, test, production
NODE_ENV=development

# sample DB URL = postgresql://{USERNAME}:{PASSWORD}@localhost:5432/{DB_NAME}
DATABASE_URL="postgresql://melvstein:password@localhost:5432/melvstein_portfolio_db"
DIRECT_URL="postgresql://melvstein:password@localhost:5432/melvstein_portfolio_db"

STAGING_DATABASE_URL=""
STAGING_DIRECT_URL=""

# Connect to Postgres via the shared transaction-mode pooler (IPv4-only)
PRODUCTION_DATABASE_URL=""

# Connect to Postgres via the shared session-mode pooler (used for migrations)
PRODUCTION_DIRECT_URL=""

DATABASE_HOST="localhost"
DATABASE_PORT=5432
DATABASE_NAME="melvstein_portfolio_db"
DATABASE_USER="melvstein"
DATABASE_PASSWORD="a123456"